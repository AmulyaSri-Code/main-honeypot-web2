# Public deployment URL used for canonical, sitemap, and social preview links.
# Replace before production deployment.
VITE_PUBLIC_SITE_URL=https://honeypot.example.com

# Optional future safe summary endpoint. Do not expose the private honeypot admin API here.
# VITE_PUBLIC_SUMMARY_API=https://api.your-domain.example/public/summary
